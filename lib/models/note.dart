///Clase Note
class Note {
  // Contructor
  Note({
    required this.key,
    required this.title,
    required this.content,
  });

  // Propiedades
  String key;
  String title;
  String content;
}
